[tool.poetry]
name = "amanda-ia"
version = "0.1.0"
description = ""
authors = ["Edgar Sánchez <edgar.sanchez@mercadolibre.cl>"]

[tool.poetry.dependencies]
python = "^3.11"
transformers = "^4.42.3"
coverage = "^7.5.4"
tensorflow = "^2.16.2"
torch = "2.2.2"
accelerate = "^0.31.0"

[tool.poetry.dev-dependencies]
pytest = "^8.2.2"
pytest-dotenv = "^0.5.2"
pytest-cov = "^5.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
